<p style="margin-top: 2.5rem"> 
    Note:
    @if($letter_type == "reminder_letter" || $letter_type == "dunning_letter")
        Kindly disregard this notice if your account was already settled.
    @else
        This is a computer-generated letter, no signature required.
    @endif
</p>